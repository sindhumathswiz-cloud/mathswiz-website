'use client';
import { useEffect, useState } from 'react';
import Link from 'next/link';
import { ArrowLeft, HeartHandshake, Loader2 } from 'lucide-react';
export default function StudentInterventionsClient() { const [items, setItems] = useState<any[]>([]); const [loading, setLoading] = useState(true); const load = () => fetch('/api/student/interventions').then(r => r.json()).then(data => setItems(data.interventions || [])).finally(() => setLoading(false)); useEffect(() => { void load(); }, []); const start = async (id: string) => { await fetch(`/api/student/interventions/${id}`, { method: 'PATCH' }); await load(); }; return <main className="min-h-screen bg-slate-50 p-6 md:p-10"><div className="mx-auto max-w-4xl"><Link href="/student/dashboard" className="mb-6 inline-flex items-center gap-2 text-sm font-bold text-indigo-700"><ArrowLeft className="h-4 w-4" />Student dashboard</Link><div className="mb-8 flex gap-3"><HeartHandshake className="h-9 w-9 text-indigo-600"/><div><h1 className="text-3xl font-black">My support plans</h1><p className="text-slate-600">Focused actions assigned by your teacher.</p></div></div>{loading ? <Loader2 className="mx-auto my-16 h-8 w-8 animate-spin"/> : items.length === 0 ? <div className="rounded-3xl border border-dashed bg-white p-16 text-center font-bold text-slate-600">No active support plans.</div> : <div className="space-y-5">{items.map(item => <article key={item.id} className="rounded-3xl border bg-white p-6"><div className="flex flex-wrap justify-between gap-2"><div><p className="text-xs font-black uppercase text-indigo-600">{item.type} · {item.topic}</p><h2 className="text-xl font-black">{item.title}</h2></div><span className="rounded-full bg-indigo-50 px-3 py-1 text-xs font-black text-indigo-700">{item.status}</span></div><p className="my-4 text-slate-700">{item.description}</p><div className="flex flex-wrap items-center justify-between gap-3 text-sm text-slate-500"><span>{item.batch?.name || 'Individual support'}{item.dueDate ? ` · Due ${new Date(item.dueDate).toLocaleDateString('en-IN')}` : ''}</span>{item.status === 'ASSIGNED' && <button onClick={() => start(item.id)} className="rounded-xl bg-indigo-600 px-4 py-2 font-bold text-white">Start support plan</button>}</div>{item.outcomeNotes && <p className="mt-4 rounded-xl bg-emerald-50 p-3 text-sm text-emerald-800"><strong>Outcome:</strong> {item.outcomeNotes}</p>}</article>)}</div>}</div></main>; }
