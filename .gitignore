.env

# Secrets (downloaded env with live API keys)
env.download

# Source textbooks (large, copyrighted) and generated chapter splits
/*.pdf
chapters/
.env.local
.env*.local

# Browser-test output
frontend/test-results/
frontend/playwright-report/

# Local package store / tooling scratch
.pnpm-store/
.codex-temp/
.codex_tmp/

# Claude Code local session config (launch.json, settings.local.json,
# scheduled_tasks.lock) -- per-machine, not project config.
.claude/

# Working exports from the question-bank second-review pass (per-question
# QA notes/decisions) -- a local review artifact regenerable from the DB,
# same treatment as qa-backups/ below.
frontend/_second_review_export/
